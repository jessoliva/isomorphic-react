describe(`The Question Detail Component`,() => {
    it(`should not regress`, () => {

    });
});

// first argument is a string which explains what it is we're testing, like the component name
// the second argument is a method, which acts as a scope for all of our tests
// need to use an it (test) statement to have a test
    // the first argument --> the thing we are testing, describe what we are doing
    // the second argument --> will be an assertion
        // to jest, an empty assertion is a passing test